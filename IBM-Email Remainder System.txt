<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>IBM Email Reminder System</title>
  <style>
    body {
      font-family: "IBM Plex Sans", Arial, sans-serif;
      background-color: #f4f7fa;
      color: #172b4d;
      padding: 2rem;
      max-width: 480px;
      margin: auto;
    }
    h1 {
      text-align: center;
      color: #0f62fe;
    }
    form {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgb(0 0 0 / 0.1);
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    label {
      font-weight: 600;
    }
    input, textarea {
      font-family: inherit;
      font-size: 1rem;
      padding: 0.5rem;
      border: 1px solid #d9d9d9;
      border-radius: 4px;
      transition: border-color 0.3s;
    }
    input:focus, textarea:focus {
      border-color: #0f62fe;
      outline: none;
    }
    button {
      background-color: #0f62fe;
      color: white;
      border: none;
      padding: 0.75rem;
      font-size: 1rem;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    button:hover {
      background-color: #0353e9;
    }
    .message {
      margin-top: 1rem;
      font-weight: 600;
    }
    .error {
      color: #da1e28;
    }
    .success {
      color: #198038;
    }
  </style>
</head>
<body>
  <h1>IBM Email Reminder System</h1>
  <form id="reminderForm">
    <label for="email">Email Address</label>
    <input type="email" id="email" required placeholder="you@example.com" />

    <label for="message">Reminder Message</label>
    <textarea id="message" rows="3" required placeholder="Enter your reminder"></textarea>

    <label for="datetime">Reminder Date & Time</label>
    <input type="datetime-local" id="datetime" required />

    <button type="submit">Set Reminder</button>
  </form>

  <div class="message" id="formMessage"></div>

  <script>
    const form = document.getElementById('reminderForm');
    const messageDiv = document.getElementById('formMessage');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      messageDiv.textContent = '';
      messageDiv.className = 'message';

      const email = document.getElementById('email').value.trim();
      const message = document.getElementById('message').value.trim();
      const datetime = document.getElementById('datetime').value;

      if (!email || !message || !datetime) {
        messageDiv.textContent = 'Please fill all fields';
        messageDiv.classList.add('error');
        return;
      }

      try {
        const response = await fetch('/api/reminder', {
          method: 'POST',
          headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({email, message, date: datetime})
        });
        const result = await response.json();

        if (result.status === 'success') {
          messageDiv.textContent = 'Reminder set successfully!';
          messageDiv.classList.add('success');
          form.reset();
        } else {
          messageDiv.textContent = result.message || 'Error setting reminder';
          messageDiv.classList.add('error');
        }
      } catch (err) {
        messageDiv.textContent = 'Network error. Try again.';
        messageDiv.classList.add('error');
      }
    });
  </script>
</body>
</html>

